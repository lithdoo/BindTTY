# @bindtty/layout

Layout tree generation for BindTTY mounted nodes.

The first stage exposes the layout package API and `BasicLayoutEngine` skeleton. Detailed element layout rules are implemented in later phases.
