# @bindtty/renderer-terminal

Terminal renderer package for BindTTY.

This package is intentionally empty for now. It reserves the package boundary for the future `LayoutNode -> Frame -> ANSI Patch` renderer pipeline.
